#include <stdio.h>

// 商品情報を表すProdct型の定義
typedef struct Product {
	char name[16]; // 商品名
	int price; // 価格
} Product;

// ここに各設問の関数を追加する


int main(void)
{
	Product p1 = { "pen", 270 };
	Product p2 = { "notebook", 160 };

	// 追加した関数を呼び出し、戻り値がある場合には出力する

}
