#include <bits/stdc++.h>
using namespace std;
int main() {
    vector<vector<char>> data(10, vector<char>(10));
    for (int i = 0; i < 10; i++){
        for (int j = 0; j < 10; j++){
            cin >> data.at(i).at(j);
        }
    }
    
}