#include <bits/stdc++.h>
using namespace std;
int main() {
    int N, K, result = 0;
    cin >> N >> K;
    int data[N];
    for (int i = 0; i < N; i++) cin >> data[i];
    
    for (int i = 0; i < N; i++){
        bitset<int> bs();
        
    }

    cout << result << endl;
}