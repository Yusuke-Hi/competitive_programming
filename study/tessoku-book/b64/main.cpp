#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int a[n + 1], b[n + 1], c[n + 1];
    for (int i = 1; i <= m; i++){
        cin >> a[i] >> b[i] >> c[i];
    }
    vector<int> graph[n + 1];
}